<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Vichitra" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">#000</item>
    </style>

    <style name="Theme.Vichitra" parent="Base.Theme.Vichitra" />
    <style name="CardView.gradient" parent="CardView">
        <item name="android:foreground">@drawable/splash_gradient</item>
    </style>
</resources>